{{
  "name": "getmerich-starter",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install --include=dev && npm --prefix client run build",
    "start": "node server/index.js",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
